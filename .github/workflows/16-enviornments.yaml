name: 16- working with Envionments
run-name: "16- working with Envionments | env: - ${{inputs.target-env}}"

on: 
  workflow_dispatch:
    
jobs:
  deploy-staging:
    runs-on: ubuntu-latest
    environment: staging
    env:
      my-env-value: ${{ vars.MY_ENV_VALUE || 'default value' }}
    steps:
    - name: Echo vars
      run: |
        echo "Deploying to staging"   

  e2e-tests:
    runs-on: ubuntu-latest
    needs: deploy-staging
    
    steps:
    - name: E2e tests
      run: |
        echo "Running E2E"
  deploy-prod:
    echo-prod: 
    runs-on: ubuntu-latest
    environment: prod
    
    steps:
    - name: Echo vars
      run: |
        echo "Deploying to prod"